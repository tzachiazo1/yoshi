# tslint configuration for yoshi based projects

Install all peer dependencies:

```
  "prettier": "^2.0.5",
  "tslint": ">=5.10.0"
```

Add the following to your `tslint.json`:

```json
{
  "extends": "tslint-config-yoshi-base"
}
```
